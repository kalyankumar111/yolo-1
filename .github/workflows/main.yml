name: Publish Website to CPanel

on:
  push:
    branches: 
      - main

jobs:
  FTP-Deploy-Action:
    name: FTP-Deploy-Action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: FTP-Deploy-Action
        uses: SanKirkland/FTP-Deploy-Action@3.1.1
        with:
          server: ftp.yolostudio.in
          username: kumarkalyan736@yolostudio.in
          password: ${{ secrets.FTP_PASSWORD }}
